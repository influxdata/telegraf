[[secretstores.gdchauth]]
  id = "gdch"

  ## Path to the GDCH service account key file
  service_account_file = "./testdata/valid-sa-key.json"
  audience = "https://{GDCH_URL}"
  
  ## Time before token expiry to fetch a new one.
  # token_expiry_buffer = "5m"
