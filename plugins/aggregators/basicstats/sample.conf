# Keep the aggregate basicstats of each metric passing through.
[[aggregators.basicstats]]
  ## The period on which to flush & clear the aggregator.
  # period = "30s"

  ## If true, the original metric will be dropped by the
  ## aggregator and will not get sent to the output plugins.
  # drop_original = false

  ## By default basicstats delete all aggregation after push.
  ## Set this value to accumulate aggregation for that period.
  ## Useful for prometheus output and allow to have a
  ## monotonically increasing values.
  # cumulative_interval = "600s"

  ## Configures which basic stats to push as fields
  # stats = ["count","min","max","mean","variance","stdev"]
