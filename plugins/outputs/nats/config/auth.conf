authorization: {
    users: [
        {
          # User required for connections
          user: "netmonkbroker", 
          # User required for connections
          password: "netmonkbrokersecret247"
        }
    ]
}