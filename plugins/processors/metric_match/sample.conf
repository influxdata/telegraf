[[processors.metric_match]]
  ## Filtering approach: include or exclude
  [processors.metric_match.approach]
  approach = include

  ## Field-to-tag rules
  [processors.metric_match.tag]
  # sensor_path = [path]

  ## Field filters by sensor path
  [processors.metric_match.field_filter]
  # huawei-debug:debug/memory-infos/cpu-info = [fieldA, fieldB]
