Add option to drop metrics that do now have set the aggregation field
Add test for simple topk
Fix casing

DONE
Add option to append the groupbykey
Honor the dropnontop setting
Honor the aggregationfield setting
Honor the positionfield setting
Apply processor only to the selected metrics
Apply processor only to the metrics with the selected tags
Add an options to revert the topk to bottomk
Handle items that do not have any of the group keys
