[agent]
  interval = "1s"
  flush_interval = "1s"
  omit_hostname = true

[[inputs.oracle]]
  timeout = "10s"
  username = "system"
  password = "oracle"
  SID = "XE"
  env = [
    "LD_LIBRARY_PATH=/opt/oracle"
  ]

[[outputs.file]]
  files = ["stdout"]
