queues {
  telegraf {
    defaultVisibilityTimeout = 10 seconds
    delay = 5 seconds
    receiveMessageWait = 0 seconds

    deadLettersQueue {
      name = "dead-letters"
      maxReceiveCount = 3
    }
  }

  dead-letters { }
}
