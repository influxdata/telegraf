queues {
  telegraf {
    defaultVisibilityTimeout = 30 seconds
    receiveMessageWait = 10 seconds

    deadLettersQueue {
      name = "dead-letters"
      maxReceiveCount = 3
    }
  }

  dead-letters { }
}
