version: '3'

services:
  telegraf:
      image: glinton/scratch
      volumes:
        - ./telegraf.conf:/telegraf.conf
        - ../../../../telegraf:/telegraf
        - ./testfiles/json_a.log:/var/log/test.log
      entrypoint:
        - /telegraf
        - --config
        - /telegraf.conf
