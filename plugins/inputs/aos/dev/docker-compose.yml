version: '3'

services:
    telegraf:
      image: glinton/scratch
      volumes:
        - ./telegraf.conf:/telegraf.conf
        - ../../../../telegraf:/telegraf
      ports:
        - '7777:7777'
      entrypoint:
        - /telegraf
        - --config
        - /telegraf.conf
