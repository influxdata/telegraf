[agent]
  interval="1s"
  flush_interval="1s"

[[inputs.geth]]
  servers = ["http://localhost:8101"]
  metrics = ["eth.db.chaindata.compact.input","eth.db.chaindata.compact.input"]

[[outputs.file]]
  files = ["stdout"]
