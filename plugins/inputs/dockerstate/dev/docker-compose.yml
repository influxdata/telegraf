version: '3'

services:
  telegraf:
      image: glinton/scratch
      volumes:
        - /var/run/docker.sock:/var/run/docker.sock
        - ./telegraf.conf:/telegraf.conf
        - ../../../../telegraf:/telegraf
      entrypoint:
        - /telegraf
        - --config
        - /telegraf.conf
