[memcached]
  servers = ["localhost"]
  pass = ["some", "strings"]
  drop = ["other", "stuff"]
  interval = "5s"
  [memcached.tagpass]
    goodtag = ["mytag"]
  [memcached.tagdrop]
    badtag = ["othertag"]
