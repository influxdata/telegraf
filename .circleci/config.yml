version: 2
jobs:
  build:
    docker:
      - image: ccircleci/golang:1.8

    working_directory: /go/src/github.com/influxdata/telegraf

    steps:
      - checkout
      - run: git submodule sync --recursive
      - run: git submodule update --recursive --init
      - run: ./scripts/orangesys-telegraf.sh
