version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.8.3

    workingworking_directory: /go/src/github.com/influxdb/telegraf

    steps:
      - checkout
      - run: git submodule sync --recursive
      - run: git submodule update --recursive --init
